# reachq runtime dependencies.
# The canonical source is [project] dependencies in pyproject.toml.
# This file exists for workflows that prefer requirements.txt.
# Install with: pip install -r requirements.txt
# For development: pip install -e ".[dev]"

numpy>=1.21.0
scipy>=1.10.0